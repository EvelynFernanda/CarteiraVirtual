import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  button: {
    backgroundColor: '#0058FF',
    padding: 12,
    marginTop: 200,
  },
  textButton: {
    color: '#FFF',
    textAlign: 'center',
    fontWeight: '500',
    fontSize: 14,
  },
});
