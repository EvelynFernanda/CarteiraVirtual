import React from 'react';
import { Text, View } from 'react-native';
import styles from './styles';

export const InitialLetter = () => {
  return (
    <View style={styles.initialLetter}>
      <Text style={styles.textInitialLetter}>M</Text>
    </View>
  );
};
