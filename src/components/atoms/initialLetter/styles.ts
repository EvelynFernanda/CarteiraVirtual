import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  initialLetter: {
    width: 60,
    height: 60,
    backgroundColor: '#05C9BF',
    borderRadius: 50,
  },
  textInitialLetter: {
    color: '#000000',
    fontSize: 24,
    fontWeight: '700',
    textAlign: 'center',
    marginTop: 12,
  },
});
